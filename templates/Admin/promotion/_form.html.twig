{{ form_start(form) }}
<ul>
    <li>
        {{ form_row(form.degree) }}
    </li>
    <li>
        {{ form_row(form.year) }}
    </li>
    <li>
        {{ form_row(form.startDate) }}
    </li>
    <li>
        {{ form_row(form.endDate) }}
    </li>
    <li>
        {{ form_row(form.notes) }}
    </li>
</ul>
<input type="submit" value="Enregistrer">
{{ form_end(form) }}

